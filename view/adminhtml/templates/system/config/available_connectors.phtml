<?php
/** @var \MageOS\DigitalSignature\Block\Adminhtml\System\Config\AvailableConnectors $block */
$connectors = $block->getConnectors();
?>
<div class="digitalsignature-available-connectors" style="padding:12px 0;max-width:760px;">
    <h4 style="margin:0 0 12px 0;font-size:1.1em;border-bottom:1px solid #ddd;padding-bottom:6px;color:#333;">
        <?= $block->escapeHtml(__('Connettori Aggiuntivi & Pianificati')) ?>
    </h4>

    <div style="display:flex;flex-direction:column;gap:16px;margin-bottom:16px;">
        <?php foreach ($connectors as $connector): ?>
            <div style="padding:12px;background-color:#f9f9f9;border:1px solid #e3e3e3;border-radius:4px;">
                <div style="display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:6px;">
                    <strong style="font-size:1.05em;color:#333;"><?= $block->escapeHtml($connector->getName()) ?></strong>
                    <?php if ($connector->getStatus() === 'in_development'): ?>
                        <span style="font-size:0.85em;font-weight:bold;color:#a85507;background-color:#fef3c7;padding:2px 6px;border-radius:4px;display:inline-block;">
                            <?= $block->escapeHtml(__('In sviluppo')) ?>
                        </span>
                    <?php else: ?>
                        <span style="font-size:0.85em;font-weight:bold;color:#15803d;background-color:#dcfce7;padding:2px 6px;border-radius:4px;display:inline-block;">
                            <?= $block->escapeHtml(__('Disponibile')) ?>
                        </span>
                    <?php endif; ?>
                </div>
                <p style="margin:0 0 8px 0;font-size:0.9em;color:#555;line-height:1.4;">
                    <?= $block->escapeHtml($connector->getDescription()) ?>
                </p>
                <?php if ($connector->getLink()): ?>
                    <a href="<?= $block->escapeUrl($connector->getLink()) ?>" target="_blank" rel="noopener noreferrer" style="font-size:0.9em;text-decoration:none;font-weight:bold;">
                        <?= $block->escapeHtml(__('Sito ufficiale del provider')) ?> &rarr;
                    </a>
                <?php endif; ?>
            </div>
        <?php endforeach; ?>
    </div>

    <div style="padding:12px;border:1px dashed #ccc;border-radius:4px;background-color:#fff;">
        <strong style="font-size:0.95em;display:block;margin-bottom:4px;color:#333;"><?= $block->escapeHtml(__('Vuoi integrare un provider differente?')) ?></strong>
        <p style="margin:0 0 6px 0;font-size:0.85em;color:#666;line-height:1.4;">
            <?= $block->escapeHtml(__('Il modulo è progettato per essere esteso in modo modulare. Puoi implementare un connettore personalizzato per qualsiasi provider di firma digitale (es. DocuSign, Namirial, InfoCert) seguendo la nostra guida.')) ?>
        </p>
        <a href="<?= $block->escapeUrl($block->getDeveloperGuideUrl()) ?>" target="_blank" rel="noopener noreferrer" style="font-size:0.85em;font-weight:bold;text-decoration:none;">
            <?= $block->escapeHtml(__('Guida all\'integrazione dei provider di firma')) ?> &rarr;
        </a>
    </div>
</div>
