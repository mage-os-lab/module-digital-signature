{
    "name": "mage-os/module-digital-signature-tests",
    "description": "Manifest minimo per eseguire la suite di unit test standalone (senza Magento reale). Uso: COMPOSER=composer.test.json composer install && vendor/bin/phpunit",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "ext-zlib": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5",
        "psr/log": "^2.0 || ^3.0"
    },
    "autoload": {
        "psr-4": {
            "MageOS\\DigitalSignature\\": "."
        }
    },
    "scripts": {
        "test": "phpunit"
    }
}
